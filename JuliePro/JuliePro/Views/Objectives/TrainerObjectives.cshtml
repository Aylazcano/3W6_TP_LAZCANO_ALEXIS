@*@model IEnumerable<TrainerObjectivesVM>

<table class="table">
    <thead>
        <tr>
            <th>Nom de l'objectif</th>
            <th>Perte de poids (kg)</th>
            <th>Distance (km)</th>
            <th>Date atteinte</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var objective in Model.Objectives)
        {
            <tr>
                <td>@objective.Name</td>
                <td>@objective.LostWeightKg</td>
                <td>@objective.DistanceKm</td>
                <td>@(objective.AchievedDate.HasValue ? objective.AchievedDate.Value.ToString("yyyy-MM-dd") : "N/A")</td>
            </tr>
        }
    </tbody>
</table>
*@